apiVersion: v1
kind: Service
metadata:
  name: springboot-service
spec:
  type: NodePort
  selector:
    app: springboot
  ports:
    - port: 8080         # Pod port
      targetPort: 8080   # Container port
      nodePort: 30007    # Port on your Windows host
